{
	"name": "icons",
	"targets": {
		"build": {
			"executor": "nx:run-commands",
			"options": {
				"command": "bun i && bun run build && bun link",
				"cwd": "drift-common/icons"
			},
			"outputs": [
				"{workspaceRoot}/dist"
			]
		},
		"link": {
			"executor": "nx:run-commands",
			"options": {
				"command": "bun link",
				"cwd": "drift-common/icons"
			},
			"dependsOn": [
				"build"
			]
		}
	}
}