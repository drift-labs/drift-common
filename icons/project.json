{
	"name": "icons",
	"targets": {
		"build": {
			"executor": "nx:run-commands",
			"options": {
				"command": "bun run build && bun link",
				"cwd": "drift-common/icons"
			},
			"outputs": [
				"{workspaceRoot}/dist"
			]
		}
	}
}