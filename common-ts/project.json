{
  "name": "common",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "bun install && bun link @drift-labs/sdk && bun run build && bun link",
        "cwd": "drift-common/common-ts"
      },
      "dependsOn": [{ "projects": "sdk", "target": "build" }],
      "outputs": ["dist"]
    }
  }
}
